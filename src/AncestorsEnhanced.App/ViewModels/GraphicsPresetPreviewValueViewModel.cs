namespace AncestorsEnhanced.App.ViewModels;

/// <summary>One concise, player-facing value shown before a graphics preset is staged.</summary>
public sealed record GraphicsPresetPreviewValueViewModel(string Name, string Value);
